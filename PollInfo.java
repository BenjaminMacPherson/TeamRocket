package dataType;

public class PollInfo {
	// size of 5 is just so it doesn't show up as an error during coding
  Location[] destinationArray = new Location[5];

// the toString might not be needed at all
  public String toString(){
	String s = "";
    return s;
  }
  
}
